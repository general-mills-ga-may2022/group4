name: dispatch workflow - ex12
on:
  workflow_dispatch
jobs:
  say_hi:
    runs-on: ubuntu-latest
    steps:
      - name: msg
        run: echo "Invoked by call_workflow"

  # Dependency to a forking workflow
  workflow_run:
    workflows: ["dispatch workflow - ex12"]
    type:
      - complete
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: msg
        run: echo "manual triger"