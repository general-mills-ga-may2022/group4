# name: amit dispatch workflow
# on:
#   workflow_dispatch
# jobs:
#   say_hi:
#     runs-on: ubuntu-latest
#     steps:
#       - name: msg
#         run: echo "Invoked by call_workflow"

name: call workflow
on:
  # Manual trigger from the UI
  workflow_dispatch:
  jobs:
    say_hi_amit:
    runs-on: ubuntu-latest
    inputs:
      message:
        description: 'call from ui'

  # Dependency to a forking workflow
workflow_run:
  workflows: ["amit dispatch workflow"]
  type:
    - complete